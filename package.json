{
  "name": "orcheweb",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "format": "prettier --write --plugin-search-dir=. . && rustywind --write ./src"
  },
  "dependencies": {
    "astro": "^1.1.5"
  },
  "devDependencies": {
    "rustywind": "^0.15.0"
  }
}
